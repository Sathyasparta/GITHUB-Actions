name: context checking
on: 
 workflow_dispatch: 
jobs: 
  context-demo:
    runs-on: ubuntu-latest
    steps:
      - name: git details
        run: echo " This is from this git id ${{ github.actor}}
             echo "this is form this reposissotry ${{github.repository}}
   
