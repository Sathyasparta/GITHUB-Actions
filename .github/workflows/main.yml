name: First Workflow

on:
  workflow_dispatch:


  # push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get Code
        uses: actions/checkout@v4

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Get Code
        uses: actions/checkout@v4
